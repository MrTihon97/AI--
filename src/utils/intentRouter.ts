/**
 * @deprecated Логика перенесена в `services/intentMatcher.ts`.
 * Файл оставлен для совместимости импортов.
 */
export {
  detectIntentId,
  isDevelopedArgument,
  isNonsenseSpam,
  LONG_TEXT_THRESHOLD,
  routeSmartReply,
  type DialogueIntent,
} from '../services/intentMatcher'
